tower_templates:
  - name: Workflow Example Unit
    description: Job Template created from code
    job_type: run
    inventory: localhost
    project: "{{ project }}"
    playbook: unit.yml
    verbosity: 1
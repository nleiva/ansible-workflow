tower_projects:
  - name: "{{ project }}"
    scm_type: git
    scm_url: "{{ repo }}"
    scm_branch: main
    scm_clean: 'no'
    scm_update_on_launch: 'no'
    description: Project created from code
    organization: "{{ organization }}"